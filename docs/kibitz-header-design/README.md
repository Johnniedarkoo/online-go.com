# Kibitz desktop split-header validation

The deterministic design fixture used during exploration has been removed
after validation in the real Kibitz application path. The retained review
material is in [real/](real/), including the three production-path screenshots,
contact sheet, environment details, and interaction checks.

The real screenshots were captured from `KibitzInner`, `KibitzRoomStage`,
`KibitzBoard`, `KibitzDesktopCompareHeader`, and actual painted Gobans. No
fixture route, CSS board illustration, or fixture-only scoreboard is required
to reproduce the production behavior.
